server:
  port: ${PORT:8080}

spring:
  application:
    name: jvm-lens

jvm-lens:
  max-steps: 500
  max-objects: 250
  max-depth: 4
  max-array-elements: 32
  max-output-bytes: 65536
  max-execution-seconds: 10